# Tests DUO reportés — REIVAX MC 0.8.5 A5

Ce lot est développé pour le SOLO et le DUO en même temps. Le test réel à deux joueurs reste reporté jusqu'à la disponibilité de Laeriss.

## À vérifier plus tard

- L'acteur reçoit sa phrase ; le partenaire reçoit la variante partenaire avec le bon nom.
- Les points et la mémoire sont ajoutés une seule fois au monde partagé.
- Deux actions rapprochées du même joueur peuvent partager un encadré.
- Les actions de deux joueurs différents ne sont jamais mélangées dans le même encadré.
- Les événements faibles peuvent attendre une intervention forte sans bloquer la file.
- Après reconnexion des deux joueurs et redémarrage complet du serveur, `/reivax_a5` conserve les validations et aucun point n'est doublé.

